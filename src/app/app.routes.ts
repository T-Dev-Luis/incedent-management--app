import { Routes } from '@angular/router';

/**
 * Application root routes.
 *
 * On day 1 only routing is enabled and verified end to end.
 * Feature routes (incidents, dashboard, authentication) will be
 * added incrementally starting on day 13 of the training plan.
 */
export const routes: Routes = [];
